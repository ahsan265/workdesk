import { ColorBackPipe } from './color-back.pipe';

describe('ColorBackPipe', () => {
  it('create an instance', () => {
    const pipe = new ColorBackPipe();
    expect(pipe).toBeTruthy();
  });
});
