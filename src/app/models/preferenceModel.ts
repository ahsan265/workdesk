export interface tickerModesl {
    mainHeading: string;
    text: string;
    tickers: tickers[];
}
export interface tickers {
    header: string,
    value: string,
    min: string,
    max: string,
}